{% extends 'base.html.twig' %}

{% block body %}
	<div class="container">
		<h1>Modifier les informations</h1>

		{{ include('experience/_form.html.twig', {'button_label': 'Update'}) }}

		<a href="{{ path('experience') }}">Retour à la liste</a>

		{{ include('experience/_delete_form.html.twig') }}
	</div>
{% endblock %}