<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{path('app_login')}}">Mettray</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarNav">
			<ul class="navbar-nav">
				<li class="nav-item">
					<a class="nav-link active" aria-current="page" href="{{ path('room') }}">Salles</a>
				</li>
				{% if app.user %}
					{% if (app.user.roles[0] is same as ("ROLE_ADMIN")) %}
						<li class="nav-item">
							<a class="nav-link" href="{{ path('booking') }}">Réservation</a>
						</li>
						<li class="nav-item">
							<a class="nav-link" href="{{ path('user_index') }}">Utilisateur</a>
						</li>
					{% endif %}
				{% endif %}
				{% if app.user %}
					<li class="nav-item">
						<a class="nav-link" href="{{ path('booking_show_all_by_user') }}">Mes réservation</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('main') }}">Mes salles</a>
					</li>
				{% endif %}
			</ul>
		</div>
		<div>
			<ul class="navbar-nav">
				{% if app.user %}
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_logout') }}">Se déconnecter</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('user_show',{'id': app.user.id}) }}">Profil</a>
					</li>
				{% else %}
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_login') }}">Se connecter</a>
					</li>

				{% endif %}
			</ul>
		</div>
	</div>
</div></nav>
