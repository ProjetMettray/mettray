{% extends 'base.html.twig' %}

{% block title %}Hello RoomController!{% endblock %}

{% block body %}
	<div class="container text-center mt-5 bg-dark">
		<h1 class="p-5 text-white">Liste des Réservation</h1>
		<div class="d-flex start-flex p-4 ">
			<label class="text-white " for="search">Recherche</label>
			<input name="search" type="text">
			<input type="submit">
		</div>
		<ul class="p-4">
			{% for room in rooms %}
				<li class="row bg-white m-2 rounded">
					<p class="col-2">{{room.name}}</p>
					<p class="col-2">{{room.getNbPlace}}</p>
					<p class="col-2">{{room.description}}</p>
					<p class="col-2">{{room.getLocation().getName()}}</p>
				</li>
			{% endfor %}
		</ul>
	</div>
{% endblock %}
