{% extends 'base.html.twig' %}

{% block title %}Hello LocationController!
{% endblock %}

{% block body %}
	<div class="container text-center mt-5 bg-dark">
		<h1 class="p-5 text-white">Liste des Réservation</h1>
		<div class="d-flex start-flex p-4 ">
			<label class="text-white " for="search">Recherche</label>
			<input name="search" type="text">
			<input type="submit">
		</div>
		<ul class="p-4">
			{% for location in locations %}
				<li class="row bg-white m-2 rounded">
					<p class="col-2">{{location.id}}</p>
					<p class="col-2">{{location.name}}</p>
					<p class="col-2">{{location.cp}}</p>
					<p class="col-2">{{location.road}}</p>
					<p class="col-2">{{location.city}}</p>
				</li>
			{% endfor %}
		</ul>
	</div>
{% endblock %}
